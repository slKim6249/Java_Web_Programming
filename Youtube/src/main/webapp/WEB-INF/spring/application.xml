<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	
	<!-- com.ktds 아래에 있는 4가지 애노테이션을 모두 가져와 스프링 빈 컨테이너에 적재시킨다
						service, repository, component, controller -->
	<context:component-scan base-package="com.ktds" />
	
	<!-- bean(Object)은 Bean Container에 적재 -->
	<bean id="viewResolver" 
		  class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/view/"/> <!-- property는 Setter 호출 , setPrefix, value는 Parameter-->
		<property name="suffix" value=".jsp"/>
	</bean>
	
	<!-- fileUpload를 사용하려면 commonsMultipartResolver Bean을 추가한다. -->
	<!-- commonsMultipartResolver를 id로 받으면 안된다. commons에 이미 있어서 겹친다. -->
	<bean id="multipartResolver"
		  class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="maxUploadSize" value="524288000" /> <!-- 500MB -->
		<property name="defaultEncoding" value="UTF-8" />
	</bean>
	
	<!-- Static Resources -->
	<mvc:annotation-driven />
	<mvc:resources location="/WEB-INF/static/js/" mapping="/js/**" /> <!-- location 마지막에 / -->
	<mvc:resources location="/WEB-INF/static/css/" mapping="/css/**" />
	<mvc:resources location="/WEB-INF/static/img/" mapping="/img/**" />
	
	
</beans>
